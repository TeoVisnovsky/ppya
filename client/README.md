# Client

This folder is a placeholder UI app for browsing scraped data.

Suggested next step: build a React/Next.js interface that consumes the server API:
- `GET /api/politicians`
- `GET /api/politicians/:id/declarations`
- `POST /api/scrape`
